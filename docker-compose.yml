# version: '3.8'
# services:
#   postgres:
#     image: postgres:15
#     container_name: postgres
#     environment:
#       POSTGRES_DB: petdb
#       POSTGRES_USER: petuser
#       POSTGRES_PASSWORD: petpass
#     ports:
#       - "5432:5432"
#     volumes:
#       - db-data:/var/lib/postgresql/data

#   keycloak:
#     image: quay.io/keycloak/keycloak:24.0.4
#     container_name: keycloak
#     command: start-dev --import-realm
#     environment:
#       KEYCLOAK_ADMIN: admin
#       KEYCLOAK_ADMIN_PASSWORD: admin
#       KC_DB: postgres
#       KC_DB_URL: jdbc:postgresql://postgres:5432/petdb
#       KC_DB_USERNAME: petuser
#       KC_DB_PASSWORD: petpass
#       KC_HOSTNAME: localhost
#       KEYCLOAK_IMPORT: /opt/keycloak/data/import/realm-export.json
#     ports:
#       - "8081:8080"
#     volumes:
#       - keycloak_data:/opt/keycloak/data
#       - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json

#   backend:
#     build:
#       context: ./Ask
#       dockerfile: Dockerfile
#     container_name: backend
#     depends_on:
#       - keycloak
#       - postgres
#       - minio
#       - mailhog
#     ports:
#       - "8080:8080"
#     environment:
#       SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/petdb
#       SPRING_DATASOURCE_USERNAME: petuser
#       SPRING_DATASOURCE_PASSWORD: petpass
#       SPRING_JPA_HIBERNATE_DDL_AUTO: update
#       GMAIL_USER: your_gmail@gmail.com
#       GMAIL_PASS: your_gmail_app_password
#       MINIO_ENDPOINT: http://minio:9000
#       MINIO_ACCESS_KEY: minioadmin
#       MINIO_SECRET_KEY: minioadmin123
#       MINIO_BUCKET: pets-images
#     volumes:
#       - ./Ask:/app

#   frontend:
#     build:
#       context: ./frontend
#       dockerfile: Dockerfile
#     container_name: frontend
#     depends_on:
#       - backend
#     ports:
#       - "8083:80"
#     environment:
#       VITE_API_URL: http://localhost:8080
#     volumes:
#       - ./frontend:/app

#   mailhog:
#     image: mailhog/mailhog
#     container_name: mailhog
#     ports:
#       - "8025:8025"
#       - "1025:1025"

#   jenkins:
#     image: jenkins/jenkins:lts
#     container_name: jenkins
#     user: root
#     ports:
#       - "8082:8080"
#       - "50000:50000"
#     volumes:
#       - jenkins_home:/var/jenkins_home
#       - /var/run/docker.sock:/var/run/docker.sock
#       - /usr/bin/docker:/usr/bin/docker
#     environment:
#       JAVA_OPTS: -Djenkins.install.runSetupWizard=false

#   minio:
#     image: minio/minio:latest
#     container_name: minio
#     environment:
#       MINIO_ROOT_USER: minioadmin
#       MINIO_ROOT_PASSWORD: minioadmin123
#     command: server /data --console-address ":9001"
#     ports:
#       - "9000:9000"
#       - "9001:9001"
#     volumes:
#       - minio-data:/data

# volumes:
#   db-data: {}
#   jenkins_home: {}
#   keycloak_data: {}
#   minio-data: {}
       


networks:
  devops-pets-network:
    driver: bridge

services:
  postgres:
    image: postgres:15
    container_name: devops-pets-postgres
    environment:
      POSTGRES_DB: petdb
      POSTGRES_USER: petuser
      POSTGRES_PASSWORD: petpass
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - devops-pets-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U petuser -d petdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-tb:
    image: postgres:15
    container_name: devops-pets-postgres-tb
    environment:
      POSTGRES_DB: thingsboard
      POSTGRES_USER: thingsboard
      POSTGRES_PASSWORD: thingsboard
    volumes:
      - tb-postgres-data:/var/lib/postgresql/data
    networks:
      - devops-pets-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U thingsboard -d thingsboard"]
      interval: 10s
      timeout: 5s
      retries: 5

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.4
    container_name: devops-pets-keycloak
    command: start-dev
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/petdb
      KC_DB_USERNAME: petuser
      KC_DB_PASSWORD: petpass
      KC_HOSTNAME: localhost
      # Uncomment the following line if you have a realm-export.json file:
      # KEYCLOAK_IMPORT: /opt/keycloak/data/import/realm-export.json
    ports:
      - "8081:8080"
    volumes:
      - keycloak_data:/opt/keycloak/data
      # Uncomment the following line if you have a realm-export.json file:
      # - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json
    depends_on:
      postgres:
        condition: service_healthy

  minio:
    image: minio/minio:latest
    container_name: devops-pets-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  mailhog:
    image: mailhog/mailhog
    container_name: devops-pets-mailhog
    ports:
      - "8025:8025"
      - "1025:1025"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8025"]
      interval: 10s
      timeout: 5s
      retries: 3

  backend:
    build:
      context: ./Ask
      dockerfile: Dockerfile
    container_name: devops-pets-backend
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      thingsboard:
        condition: service_started
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/petdb
      SPRING_DATASOURCE_USERNAME: petuser
      SPRING_DATASOURCE_PASSWORD: petpass
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      # Gmail SMTP Configuration
      GMAIL_USER: ${GMAIL_USER:-}
      GMAIL_PASS: ${GMAIL_PASS:-}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin123
      MINIO_BUCKET: pets-images
      # Gmail SMTP Settings
      SPRING_MAIL_HOST: smtp.gmail.com
      SPRING_MAIL_PORT: 587
      SPRING_MAIL_AUTH: "true"
      SPRING_MAIL_STARTTLS: "true"
      SPRING_MAIL_STARTTLS_REQUIRED: "true"
      FRONTEND_URL: http://localhost:8083
      THINGSBOARD_URL: http://thingsboard:8080
      THINGSBOARD_TOKEN: ${THINGSBOARD_TOKEN:-}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || curl -f http://localhost:8080/api || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: devops-pets-frontend
    depends_on:
      backend:
        condition: service_started
    ports:
      - "8083:80"
    environment:
      VITE_API_URL: http://localhost:8080
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s


  rabbitmq:
    image: rabbitmq:3-management
    container_name: devops-pets-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=password123
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - devops-pets-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  node-red:
    image: nodered/node-red:latest
    container_name: devops-pets-node-red
    ports:
      - "1880:1880"
    environment:
      - TZ=Europe/Athens
      - NODE_RED_CREDENTIAL_SECRET=devops-pets-secret-key-change-in-production
    volumes:
      - node-red-data:/data
    networks:
      - devops-pets-network
    depends_on:
      rabbitmq:
        condition: service_healthy
      thingsboard:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:1880"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  thingsboard:
    image: thingsboard/tb
    container_name: thingsboard
    restart: always
    ports:
      - "9090:8080"
      - "1883:1883"
    environment:
      - TB_QUEUE_TYPE=rabbitmq
      - TB_QUEUE_RABBIT_MQ_HOST=rabbitmq
      - TB_QUEUE_RABBIT_MQ_PORT=5672
      - TB_QUEUE_RABBIT_MQ_USER_NAME=rabbitmq
      - TB_QUEUE_RABBIT_MQ_PASSWORD=rabbitmq123
      - TB_QUEUE_RABBIT_MQ_VIRTUAL_HOST=/
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-tb:5432/thingsboard
      - SPRING_DATASOURCE_USERNAME=thingsboard
      - SPRING_DATASOURCE_PASSWORD=thingsboard
      # Ensure Spring uses PostgreSQL driver/dialect (fix HSQLDB driver error)
      - SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.PostgreSQLDialect
      - SPRING_DRIVER_CLASS_NAME=org.postgresql.Driver
      - SERVER_PORT=8080
    volumes:
      - tb-data:/data
      - tb-logs:/var/log/thingsboard
    networks:
      - devops-pets-network
    depends_on:
      rabbitmq:
        condition: service_healthy
      postgres-tb:
        condition: service_healthy

      
volumes:
  db-data:
    driver: local
  keycloak_data:
    driver: local
  minio-data:
    driver: local
    

  tb-data:
  tb-logs:
  tb-postgres-data:
    driver: local
  rabbitmq-data:
    driver: local
  node-red-data:
    driver: local